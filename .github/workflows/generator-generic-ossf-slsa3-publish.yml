name: Deploy VPS
on: [push]
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: pull changes to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          script: |
            cd ~/home/risyafin/hello-wordl
            git pull origin master
